# Task 3: Image Captioning AI

This project combines Computer Vision and Natural Language Processing to generate captions for images.

A pre-trained image recognition model is used to extract image features, and a neural network generates descriptive captions.

Technologies Used:
- Python
- Deep Learning
- CNN (for image feature extraction)
- RNN / Transformer (for caption generation)
